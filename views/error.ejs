<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= code %> - <%= error %> | AnimaKU</title>
    <!-- Favicon -->
    <link
      rel="shortcut icon"
      href="/images/icons/favicon.ico"
      type="image/x-icon"
    />

    <!-- Remix Icon -->
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.6.0/fonts/remixicon.css"
      rel="stylesheet"
    />

    <!-- CSS Stuff -->
    <link rel="stylesheet" href="/css/tailwind.css" />
    <link rel="stylesheet" href="/css/styles.css" />

    <!-- Static JS -->
    <script src="/js/navbar.js" defer></script>
    <script src="/js/browse.js" defer></script>
    <script src="/js/modal.js" defer></script>
  </head>
  <body>
    <header><%- include('partials/navbar.ejs') %></header>

    <main
      class="grid min-h-full place-items-center px-6 py-24 sm:py-32 lg:px-8"
    >
      <div class="text-center">
        <p class="text-base font-semibold text-[#88c0d0]"><%= code %></p>
        <h1
          class="mt-4 text-5xl font-semibold tracking-tight text-balance text-[#eceff4] sm:text-7xl"
        >
          <%= error %>
        </h1>
        <p
          class="mt-6 text-lg font-medium text-pretty text-gray-400 sm:text-xl/8"
        >
          <%= description %>
        </p>
        <div class="mt-10 flex items-center justify-center gap-x-6">
          <a
            href="/"
            class="rounded-md bg-[#88c0d0] px-3.5 py-2.5 text-sm font-semibold text-[#2e3440] shadow-xs hover:bg-[#94d0e0] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-500"
            >Go back home</a
          >
        </div>
      </div>
    </main>
  </body>
</html>

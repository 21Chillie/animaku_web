<!DOCTYPE html>
<html lang="en" data-theme="dark">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">

	<link rel="shortcut icon" href="/images/icons/favicon.ico" type="image/x-icon" />

	<!-- CSS -->
	<link rel="stylesheet" href="/css/tailwindcss/tailwind.css" />
	<link href="https://cdn.jsdelivr.net/npm/remixicon@4.7.0/fonts/remixicon.css" rel="stylesheet" />
	<link rel="stylesheet" href="/css/error.css" />

	<title><%= error %></title>

	<script type="module" src="/js/navbar.js" defer></script>
	<script type="module" src="/js/toggleTheme.js" defer></script>
	<title>Error Page</title>
</head>

<body>
	<header>
		<%- include('partials/navbar.ejs') %>
	</header>

	<main class="max-w-7xl mx-auto mt-28 md:mt-32 px-4 text-center">
		<!-- Error Code -->
		<section aria-labelledby="error-code" class="relative mb-8">
			<h1 id="status-code"
					class="text-8xl md:text-9xl font-extrabold leading-none opacity-20 select-none"
					aria-hidden="true">
				<%= status_code %>
			</h1>

			<div class="absolute inset-0 flex items-center justify-center">
				<span id="status-code-small"
						class="text-5xl md:text-6xl font-bold"><%= status_code %></span>
			</div>
		</section>

		<!-- Message -->
		<section class="mb-8 space-y-4">
			<h2 id="error" class="text-2xl md:text-3xl font-bold">
				<%= error %>
			</h2>

			<p class="message max-w-xl mx-auto text-sm md:text-base leading-relaxed opacity-80">
				<%= message %>
			</p>
		</section>

		<!-- Actions -->
		<nav class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-10">
			<a href="/" class="btn btn-outline flex items-center gap-2">
				<i class="ri-home-5-line"></i>
				<span>Back to Home</span>
			</a>
		</nav>

		<!-- Meta Info -->
		<% if (path) { %>
		<footer class="text-sm">
			<span class="path-message">Attempted path:</span>
			<code class="px-2 py-1 rounded-md inline-block">
				<%= path %>
			</code>
		</footer>
		<% } %>
	</main>
</body>

</html>
